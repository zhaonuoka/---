{
    "name": "prometheus-avatar",
    "private": true,
    "description": "AI Avatar Embodied Intelligence Framework — Open Source SDK",
    "scripts": {
        "build": "pnpm -r build",
        "lint": "pnpm -r lint",
        "clean": "pnpm -r clean"
    },
    "engines": {
        "node": ">=18.0.0",
        "pnpm": ">=8.0.0"
    },
    "packageManager": "pnpm@9.15.0",
    "dependencies": {
        "jsonwebtoken": "^9.0.3"
    }
}
